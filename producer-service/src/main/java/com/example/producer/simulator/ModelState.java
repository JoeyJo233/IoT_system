package com.example.producer.simulator;

/** Mutable per-sensor state for stateful DataModel variants (RANDOM_WALK, STEP). */
public class ModelState {
    public Double value;
    public Integer tickCount;
}
